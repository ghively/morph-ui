# Extraction source snapshot (2026-09-20)

Reference-only copies of ChatUIMorph runtime/web/src UI files, staged for
the morph-ui componentization campaign. NOT shipped library code — do not
import from here. Builders convert these into generic components under
src/components/ per docs/extraction-specs.md; this directory is removed
when the campaign completes.
